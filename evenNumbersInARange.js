let firstNumber=11;
let lastNumber=20;
console.log("Even numbers between "+firstNumber+" & "+lastNumber +" are ");
for(let number=firstNumber;number<=lastNumber;number++){
  if(number%2==0){
    console.log(number);
  }
}
